# Tarefa

Carregue o CSV, padronize nomes de colunas, trate nulos em `valor`,
crie uma coluna `mes` a partir de `data`, agregue por `mes` somando `valor`,
gere um gráfico de linha e salve em `out/plot.png`.
No final salve também a tabela agregada em `out/resumo.csv`.
